set(DEPLOYQT "@DEPLOYQT@")
set(BINARIES "SKVM/data/bin")
set(CMAKE_CURRENT_SOURCE_DIR "@CMAKE_CURRENT_SOURCE_DIR@")
set(PACKAGES "${CPACK_TEMPORARY_INSTALL_DIRECTORY}/packages")

execute_process(COMMAND ${DEPLOYQT} --qmldir ${CMAKE_CURRENT_SOURCE_DIR} ${BINARIES} WORKING_DIRECTORY ${PACKAGES})
